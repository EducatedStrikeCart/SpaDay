@using SpaDay.ViewModel

@model AddUserViewModel

@{
}

<h1>Sign Up Form</h1>

@*@if (ViewBag.error != null)
{
    <p>@ViewBag.error</p>
}*@

<span asp-validation-for="Username"></span>
<span asp-validation-for="Email"></span>
<span asp-validation-for="Password"></span>

<form method="post" action="/user">
    <div class="form-group">
        <label asp-for="Username"></label>
        <input asp-for="Username" />
        <span asp-validation-for="Username"></span>
    </div>

    <div class="form-group">
        <label asp-for="Email">Email</label>
        <input asp-for="Email" />
        <span asp-validation-for="Email"></span>
    </div>

    <div class="form-group">
        <label asp-for="Password">Password</label>
        <input asp-for="Password" type="password" />
        <span asp-validation-for="Password"></span>
    </div>

    <div class="form-group">
        <label asp-for="VerifyPassword">Verify Password</label>
        <input asp-for="VerifyPassword" type="password" />
        <span asp-validation-for="VerifyPassword"></span>
    </div>



    @*Username:
        <br>
        @if (ViewBag.error == null)
        {
            <input type="text" name="username" />
        }
        else
        {
            <input type="text" name="username" value="@ViewBag.userName" />
        }
        <br>
        Email:
        <br>
        @if (ViewBag.error == null)
        {
            <input type="email" name="email" />
        }
        else
        {
            <input type="email" name="email" value="@ViewBag.eMail" />
        }
        <br>
        Password:
        <input type="password" name="password" />
        <br>
        Confirm Password:
        <br>
        <input type="password" name="verifypassword" />
        <br>*@
    <input type='submit' value='Submit'>
</form>
